# Trivolta — Session Handoff (2026-05-04)

## State of the project

The project is healthy. Do not let previous session confusion affect confidence. Here is the exact current state.

### What just happened this session

1. **Discovered** that the 3,976 questions in the `facts` table came from The Trivia API, which is non-commercial licensed. They cannot be used in a commercial app.
2. **Decided** to replace them with OpenTrivia DB questions (CC BY-SA 4.0, commercial use allowed).
3. **Replaced** all Trivia API questions. 3,285 OpenTrivia DB questions are now in the local `facts` table. 26/26 Maestro tests pass. Review verdict: comment (no blockers).
4. **Decided on architecture direction** for gameplay: replace `solo-question` (which calls Anthropic on every game, never touches the fact bank) with a simple DB query against the `facts` table. No AI paraphrasing layer. No `render-question`. No `question_renderings` table. Pure DB read, fast and free.
5. **The F4/F5/F6 render-question architecture is cancelled.** `INSTRUCTIONS_PHASE_2.6.4_RENDER_AND_COMPOSE.md` exists on disk as historical reference but is superseded. Do not execute it.

---

## What to do next

### Immediate task: Replace `solo-question` with DB fact lookup

The next INSTRUCTIONS file to write is `INSTRUCTIONS_REPLACE_SOLO_QUESTION.md`.

**What it should accomplish:**
- Modify the `solo-question` Edge Function (or replace it) so that instead of calling Anthropic to generate a question from scratch, it:
  1. Takes `category_slug` from the request
  2. SELECTs a random fact from the `facts` table for that category, filtered to `source_origin = 'opentdb_import'`
  3. Excludes facts the player has seen recently (use `fact_exposures` table if it has data; if not, skip for now — can be added later)
  4. Returns the question, correct answer, and distractors in the same shape the mobile app already expects
- Zero Anthropic calls during solo gameplay
- The mobile app (`mobile/app/question.tsx` and `mobile/lib/api.ts`) should NOT need to change — the response shape must be identical to what `solo-question` returns today
- 26/26 Maestro tests must still pass

**Critical constraint:** Check the exact response shape that `solo-question` currently returns before writing the INSTRUCTIONS file. Read `supabase/functions/solo-question/index.ts` and `mobile/lib/api.ts` to confirm the expected field names. The mobile app must receive the same shape — do not break the contract.

**Also check:** What shape does the `distractors` table store? Verify column names before writing the INSTRUCTIONS. The fact bank stores: `fact_text` (the question), `correct_answer`, and separate rows in `distractors` for wrong answers.

---

## Architecture decisions made this session (do not re-debate)

| Decision | What was decided |
|---|---|
| Question source | OpenTrivia DB (CC BY-SA 4.0). 3,285 facts in local DB. |
| AI generation during gameplay | Eliminated for solo play. Pure DB read. |
| Render/paraphrase layer (F4/F5/F6) | Cancelled. Not needed. |
| Hallucination risk | Resolved by using pre-verified human-authored questions from OpenTrivia DB. |
| Wikidata / jService / other sources | Explored and deferred. OpenTrivia DB is sufficient for beta. |
| App direction | Ship to TestFlight. Get real users. Stop debating strategy. |

---

## Current facts table

```
slug          | count
--------------+-------
general       | 641
pop-culture   | 540
film          | 423
music         | 391
history       | 391
geography     | 306
science       | 275
sports        | 160
literature    | 109
art           | 49
TOTAL         | 3,285
```

All have 3 distractors each. All are `source_origin = 'opentdb_import'`, `verification_status = 'pending'`.

---

## After solo-question is replaced

The next task after that is replacing lobby question generation (`generate-questions`) with the same DB fact lookup pattern. That is `INSTRUCTIONS_REPLACE_LOBBY_QUESTIONS.md`.

After both are done, gameplay makes zero Anthropic calls. Cost to serve a game: $0.

After that: production Supabase setup, then EAS Build, then TestFlight.

---

## Last commit state

- Branch: `main`
- Last commits: `fac7b56` (OpenTrivia DB import), `050c867` (simplify revert artifact)
- Review verdict on HEAD: `comment`, 0 blockers
- 26/26 Maestro tests passing
- Working tree: clean (verify with `git status` before starting)

---

## Files to read at session start

```
Filesystem:read_multiple_files with:
- CLAUDE.md
- WORKFLOW.md
- TRIVOLTA_TRACKER.md
- supabase/functions/solo-question/index.ts
- mobile/lib/api.ts
- supabase/migrations/20240106000000_fact_bank_schema.sql
```

That is everything needed to write `INSTRUCTIONS_REPLACE_SOLO_QUESTION.md`.
