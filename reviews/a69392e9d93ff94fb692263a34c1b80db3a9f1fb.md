---
commit: a69392e9d93ff94fb692263a34c1b80db3a9f1fb
branch: main
instructions_file: INSTRUCTIONS_F3_FACT_SPOT_CHECK.md
reviewer_model: claude-sonnet-4-6
verdict: request_changes
findings_count: 3
blockers_count: 2
generated_at: 2026-05-01T23:35:00Z
---

# Code review — a69392e

## Verdict

Two blockers prevent approval. The commit message reports "27/27 passing" where the spec requires 28/28 — and reading `run_tests.sh` and `supabase/seed.sql` together confirms why: the suite uses a glob that includes test_28, but `seed.sql` is empty (0 bytes), so after `supabase db reset` there are no facts or distractors in the DB and test_28 cannot reach its first assertion. Additionally, five files outside F3's authorized scope appear in the diff, violating the "No unintended files modified" criterion.

## Findings

1. [blocker] Maestro count mismatch and test_28 ungated pre-condition. Commit message reports "27/27 passing" but the verifiable objective requires `./run_tests.sh` to report 28/28. `run_tests.sh` uses `for f in maestro/test_*.yaml; do` — test_28 is included in the glob when the file is present, so a run that produced "27/27" either excluded test_28 or test_28 failed and was omitted from the reported count. The root cause: `supabase/seed.sql` is empty; after `supabase db reset`, there are no facts and no distractors. `get_next_spot_check_fact()` returns no rows, the screen renders the empty state, and test_28 times out on `spot-check-fact-text`. test_28 needs a `runScript` step that seeds at least one fact with three active distractors (via the service role API) before navigating to the spot-check screen — analogous to how `ensure_test_user_02.js` ensures user pre-conditions. Without this, "single run passes after supabase db reset" no longer holds.

2. [blocker] Five files outside F3 scope added to the commit: `INSTRUCTIONS_AUTOMATED_REVIEW.md`, `INSTRUCTIONS_F2_FEEDBACK_CHANNEL.md`, `reviews/.gitkeep`, `reviews/PROMPT.md`, `reviews/README.md`. These implement (partially) the automated review pipeline — a separate future task tracked as `⬜ INSTRUCTIONS_AUTOMATED_REVIEW.md` in the tracker. The F3 spec authorized exactly: the migration, the Edge Function, the admin screen, `_layout.tsx` and `index.tsx` edits, `api.ts` edit, `ensure_admin_test_user.js`, `test_28_spot_check.yaml`, `TRIVOLTA_TRACKER.md`, and `INSTRUCTIONS_F3_FACT_SPOT_CHECK.md`. None of the five extra files appear in that list.

3. [suggestion] test_28 fact-bank pre-condition should be made self-contained. Beyond the blocker above, ensuring this is documented: CLAUDE.md's "All Tests Are Self-Contained" norm and the "single run passes after supabase db reset" guarantee will both break for test_28 unless facts are seeded in-flow. The fix is a `runScript` step at the start of test_28 analogous to `ensure_test_user_02.js` — a minimal `ensure_spot_check_facts.js` that inserts one category, one fact (`verification_status = 'pending'`), and three active distractors via the service role REST API, idempotently (skip if already present).

## Constraint compliance

- [ ] Objective met — F3 tooling is correctly implemented, but the verifiable objective "28/28 passing" is unconfirmed (commit message reports 27/27; test_28 cannot pass on a fresh DB).
- [x] Constraints not violated — single insert path enforced; `reviewer_id` comes from the JWT, not the request body; no UPDATE/DELETE policies on `spot_check_results`; no new third-party dependencies; spot-check screen is admin-nav only; Skip does not write a row; no existing Edge Functions, tables, or Maestro YAML files modified; no bulk-action buttons shipped.
- [ ] No unintended files modified — `INSTRUCTIONS_AUTOMATED_REVIEW.md`, `INSTRUCTIONS_F2_FEEDBACK_CHANNEL.md`, `reviews/.gitkeep`, `reviews/PROMPT.md`, and `reviews/README.md` are all new files in this commit and none were authorized by the F3 spec.
- [x] CLAUDE.md additions justified — no CLAUDE.md changes appear in this diff.

## Spec coverage

All schema items are present: `spot_check_results` table with correct columns, unique constraint on `(fact_id, reviewer_id)`, two indexes, RLS enabled, `spot_check_admin_read` and `spot_check_reviewer_read_own` SELECT policies, no INSERT/UPDATE/DELETE policies. The Edge Function implements the full auth preamble (CORS, Authorization → 401, apikey-header-with-env-fallback, `getUser()` → 401, is_admin → 403), field validation with correct 400 shape, fact lookup with 404, 409 on unique violation, dual-insert for `incorrect` verdict with non-rollback on secondary failure, and correct response shapes. The admin screen covers all eleven required testIDs, progress chip, category badge, fact text, correct answer (green), distractor list, correct/incorrect buttons, inline note input, skip link, 1.5s confirmation banner, and error handling — all styled with theme tokens. The `get_next_spot_check_fact()` SECURITY DEFINER RPC uses `is_admin()` guard, eligibility filters, stratification, and ≥3 distractor check. `submitSpotCheck` in `api.ts` handles 409 locally. Tracker updates (F3 ✅, test_28 active, deferred renumbered to test_29–32, F3 session sub-section with SQL) are all present. Two items are unresolved: 28/28 test confirmation and the five unauthorized files.
