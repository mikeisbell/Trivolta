I have everything I need. Here is the review:

---
commit: 35a0865da74266009ffe37ba53f1eecbf8ca2b49
branch: main
instructions_file: /Users/mizzy/Developer/Trivolta/INSTRUCTIONS_F3_BLOCKER1_TEST_28_SEED.md
reviewer_model: claude-sonnet-4-6
verdict: comment
findings_count: 3
blockers_count: 0
generated_at: 2026-05-02T07:18:00Z
---

# Code review — 35a0865

## Verdict

Comment. The commit being reviewed is the "simplify ran clean" chore on top of the implementation chain (`0d82516` fix → `36eb850` simplify changes → `35a0865` simplify verification log). With full repo access I read the actual deliverables — `ensure_spot_check_facts.js`, the updated `test_28_spot_check.yaml`, the calibration note in `reviews/README.md`, and the tracker entries in `TRIVOLTA_TRACKER.md`. All Steps A–F are satisfied; no blockers found. Three nits follow.

## Findings

1. [nit] `ensure_spot_check_facts.js` seeds **3 facts** instead of the **1** specified in Step B. The deviation is necessary and the header comment explains why correctly: `spot_check_results` has a `unique(fact_id, reviewer_id)` constraint, so after the first verdict (correct) the first fact is consumed and `get_next_spot_check_fact()` needs a second eligible fact for the incorrect-verdict assertion. One fact would cause Step D's second-verdict path to hit the empty state again. Three is the right minimum (2 needed + 1 buffer). The deviation should ideally have been called out in a diff note or a revision to the INSTRUCTIONS rather than silently absorbed, but the implementation is more correct than the spec.

2. [nit] The calibration note in `reviews/README.md` does not include the fix commit SHA. The Step E "real bug" template says: "Fix landed in commit `<sha>` adding `ensure_spot_check_facts.js` runScript step to test_28." The note mentions `ensure_spot_check_facts.js` and the runScript step but omits the SHA, making the audit trail one lookup harder to follow from the note alone.

3. [nit] The distractor partial-insert indexing has a silent duplicate risk. When `have > 0` and `have < DISTRACTORS_PER_FACT`, the insert loop picks `seed.distractors[d]` starting at index 0, not at index `have`. On a re-run where exactly 1 distractor was previously inserted, `need = 2` and the loop inserts `seed.distractors[0]` (which duplicates the row already present) and `seed.distractors[1]`. The `distractors` table has no unique constraint on `(fact_id, distractor_text)`, so the insert succeeds, leaving a duplicate row. The test still passes (`count >= 3` is satisfied), but the seed state is inconsistent. The pure fresh-DB path and the fully-seeded idempotent path are both correct; only the partial-seed path is affected.

## Constraint compliance

- [x] Objective met
- [x] Constraints not violated
- [x] No unintended files modified
- [x] CLAUDE.md additions justified

## Spec coverage

Step A (verify first): confirmed real failure; documented in `reviews/README.md` calibration notes. Step B (seeding script): `mobile/maestro/scripts/ensure_spot_check_facts.js` exists; uses Maestro's `http` object (matching the actual `ensure_test_user_02.js` pattern, which the INSTRUCTIONS nominated as the reference); service-role REST only; idempotent; header comment covers purpose, env vars, and idempotency guarantee; seeds 3 facts/9 distractors (see finding 1). Step C (test_28 update): `runScript` step for `ensure_spot_check_facts.js` inserted after `ensure_admin_test_user.js`, env-var passthrough matches project convention, no existing assertions altered. Step D (re-verify): calibration note records confirmed fix with both verdict paths exercised. Step E (calibration note): `reviews/README.md` has calibration section at bottom; multi-paragraph format exceeds the one-paragraph template but improves quality; SHA missing (see finding 2). Step F (tracker): `TRIVOLTA_TRACKER.md` has both required entries — the fix line under the workflow infrastructure block and the checked-off `INSTRUCTIONS_F3_BLOCKER1_TEST_28_SEED.md` in the INSTRUCTIONS Files Written section.
