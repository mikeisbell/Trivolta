I have all the information I need. Let me write the review.

---
commit: 3e78570e5a4cc4887a0e9800459c514b8f7f51cb
branch: main
instructions_file: /Users/mizzy/Developer/Trivolta/INSTRUCTIONS_F3_FOLLOWUP_FIXES.md
reviewer_model: claude-sonnet-4-6
verdict: comment
findings_count: 2
blockers_count: 0
generated_at: 2026-05-02T19:52:00Z
---

# Code review — 3e78570

## Verdict

Comment. The diff is a `/simplify` chore commit that adds only the forensic log `reviews/3e0b5d8.simplify-log.md`. The actual implementation work landed in `0821da0` (three fixes + test_28 simplification) and `3e0b5d8` (calibration-note SHA substitution). All five implementation items and all three tracker items are verified correct from the current repo state. Two nits follow; neither is a blocker.

## Findings

1. [nit] `mobile/run_tests.sh:20-22` — the `set -o pipefail` justification spans three lines. Both CLAUDE.md ("one short line max") and the INSTRUCTIONS ("document the choice with a one-line comment") specify a single line. The content is legitimate WHY-not-WHAT, but the verbosity violates the project style rule. Trimming to one line fixes it, e.g.: `# pipefail: report maestro's exit, not tee's — without this the pipe silently passes every failed test.`

2. [nit] `TRIVOLTA_TRACKER.md:286` — `⬜ run_tests.sh exit-code masking — folded into Phase 2.6.7.` remains under `## Deferred Pre-Beta Items` even though the issue is resolved. The INSTRUCTIONS only required removing the matching line from `## Known Issues / Tech Debt` (done), so this is outside task scope. The stale deferred entry will mislead future readers into thinking this work is still pending.

## Constraint compliance

- [x] Objective met — Fix 1 (`set -o pipefail` at `run_tests.sh:23`), Fix 2 (`keyboardShouldPersistTaps="handled"` at `spot-check.tsx:140`), Fix 3 (`seed.distractors[have + d]` at `ensure_spot_check_facts.js:141` with the specified comment at lines 136–138), test_28 keyboard-dismissal workaround removed and replaced with the specified explanatory comment at lines 80–81, all three calibration-note "Resolved" lines present with correct SHAs, and all three tracker items completed (workflow-infrastructure entry, INSTRUCTIONS-files entry, Known Issues removal).
- [x] Constraints not violated — `set -e` not added; no schema changes; no other YAML files modified; no newly-visible failures touched; no new dependencies; CLAUDE.md and WORKFLOW.md unchanged.
- [x] No unintended files modified — the diff touches only `reviews/3e0b5d8.simplify-log.md`, which is the expected artifact for a `/simplify` chore commit.
- [x] CLAUDE.md additions justified — the diff adds no CLAUDE.md changes.

## Spec coverage

All verifiable-objective items are satisfied. Fix 1 (`run_tests.sh` pipefail guard) is present and documented. Fix 2 (`keyboardShouldPersistTaps`) is present on the sole `ScrollView` and no other screen was touched. Fix 3 (partial-seed index) is `have + d` with the specified comment. test_28 no longer contains the tap-elsewhere workaround; the explanatory note replaces it. The calibration notes in `reviews/README.md` carry "Resolved" lines for all three findings with the correct SHAs (`0d82516` for finding 1, `0821da0` for findings 2 and 3). The tracker reflects the F3 follow-up as complete, the INSTRUCTIONS file as written, and the Known Issues line as removed.
